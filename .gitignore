# Node/Dev junk
/node_modules/
*.log
.DS_Store
.vscode/
.idea/
*.zip
*.tar.gz

# Build outputs we DO commit
# (Don't ignore assets/dist — we want compiled files tracked)
# Optionally ignore only noisy build artefacts:
assets/dist/**/*hot-update*
assets/dist/**/.DS_Store

# WordPress-specific (shouldn't really exist in a plugin, but harmless)
/wp-config.php
/.htaccess

# Development caches
.sass-cache/
/.pnp
.pnp.js

# Testing
/coverage/
phpunit.xml
.phpunit.cache

# Environment files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
